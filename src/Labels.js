import _ from "lodash";
import React, { useRef, useEffect } from "react";
import styles from "./sass/styles.module.sass";

const Labels = ({ skillList }) => {
  return <nav className={styles["labels"]} />;
};

export default Labels;
